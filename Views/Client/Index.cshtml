    <div class="col-12">
        <div class="d-flex justify-content-between align-items-center">
            <h1 class="mt-3 mb-3">Lista posts</h1>
            <div>
                <input type="text" id="search" class="form-control" placeholder="Cerca per titolo" onkeyup="loadPosts(this.value)" />
            </div>
        </div>
 
        <div id="post-table">
            <table class="table post-list-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Immagine</th>
                        <th>Titolo</th>
                        <th>Descrizione</th>
                        <th></th>
                        <th></th>
                    </tr>
                </thead>
                <tbody id="posts">
                    <!-- qui ci andranno i <tr> contenenti i post -->
               
                </tbody>
            </table>
        </div>
        <div id="no-posts" class="d-none">
            <h2>Non ci sono post da mostrare</h2>
        </div>
    <a class="btn btn-primary" href="/Client/Create">Nuovo post</a>
    </div>

    @section Scripts {
        <script type="text/javascript" src="~/js/API/index.js"></script>
    }
